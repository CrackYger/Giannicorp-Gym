// capacitor.config.ts
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.giannicorp.fitness',
  appName: 'GianniCorpFitness',
  webDir: 'dist'
};

export default config;
